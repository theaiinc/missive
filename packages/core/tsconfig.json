{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    // DOM supplies the WebCrypto types (CryptoKey, subtle) crypto.ts needs; the
    // same API exists in Node 20+ and Workers.
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src"],
  "exclude": ["src/__tests__"]
}
